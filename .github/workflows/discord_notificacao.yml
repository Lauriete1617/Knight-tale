- name: Test Webhook
  run: |
    echo "Webhook URL length: ${#DISCORD_WEBHOOK}"
    echo "Webhook starts with: $(echo $DISCORD_WEBHOOK | cut -c1-50)"
  env:
    DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
