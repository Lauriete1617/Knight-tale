name: Notificar Discord sobre Commits

on:
  push:
    branches:
      - main  # Mude para a branch que deseja monitorar

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Enviar notificação para Discord
        run: |
          curl -X POST ${{ secrets.DISCORD_WEBHOOK }} \
            -H "Content-Type: application/json" \
            -d '{
              "username": "GitHub Bot",
              "avatar_url": "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png",
              "embeds": [
                {
                  "title": "📝 Novo Commit",
                  "description": "Branch: `${{ github.ref_name }}`",
                  "color": 5814783,
                  "fields": [
                    {
                      "name": "Autor",
                      "value": "${{ github.actor }}",
                      "inline": true
                    },
                    {
                      "name": "Repositório",
                      "value": "${{ github.repository }}",
                      "inline": true
                    },
                    {
                      "name": "Mensagem do Commit",
                      "value": "${{ github.event.head_commit.message }}",
                      "inline": false
                    },
                    {
                      "name": "Arquivos Alterados",
                      "value": "${{ github.event.head_commit.modified_size }} modificados",
                      "inline": true
                    }
                  ],
                  "url": "${{ github.event.head_commit.url }}"
                }
              ]
            }'
